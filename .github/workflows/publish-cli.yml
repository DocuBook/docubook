name: Publish @docubook/cli to npm

on:
  push:
    tags:
      - "cli-v*.*.*"

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    name: Build (Bun)

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Bun
        uses: oven-sh/bun-action@v2

      - name: Install dependencies
        run: bun install

      - name: Build
        run: bun run build

      - name: Lint
        run: bun run lint

      - name: Type check
        run: bun run typecheck

  publish:
    needs: build-and-test
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "pnpm"
          registry-url: "https://registry.npmjs.org"
          scope: "@docubook"
          always-auth: true

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Publish @docubook/cli to npm
        run: pnpm publish packages/cli --access public --no-git-checks
        env:
          # NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }} # Not needed when using setup-node with registry-url and scope
          NPM_CONFIG_PROVENANCE: true

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            packages/cli/package.json
            packages/cli/README.md
          draft: false
          prerelease: ${{ contains(github.ref, 'alpha') || contains(github.ref, 'beta') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
