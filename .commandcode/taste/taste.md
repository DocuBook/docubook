# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# communication

- Use English (default language) for responses, not Indonesian. Confidence: 0.65

# architecture

- For packages/template/native: Use vanilla React + browser APIs (e.g., window.location.pathname)
  instead of Next.js APIs (e.g., usePathname, useRouter from next/navigation). Do not blindly copy
  from apps/web. Confidence: 0.70
